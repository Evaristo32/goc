spring:
  application:
    name: goc

  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5437}/${DB_NAME:goc}}
    username: ${DB_USER:goc}
    password: ${DB_PASSWORD:goc_pass}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        id:
          new_generator_mappings: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baselineOnMigrate: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.boot.autoconfigure.flyway: DEBUG
